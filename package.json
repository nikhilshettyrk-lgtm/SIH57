{
  "name": "marine-debris-ai-detector-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Marine Debris AI Detector - AI-powered Side-Scan Sonar anomaly detection prototype",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
